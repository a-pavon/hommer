{
  "name": "hommer-ar",
  "version": "1.0.0",
  "description": "AR Web App using mobile device camera with 3D object placement",
  "private": true,
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "dependencies": {
    "three": "^0.170.0"
  }
}
